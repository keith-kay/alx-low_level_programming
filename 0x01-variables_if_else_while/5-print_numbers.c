#include <stdio.h>

#include <stdlib.h>

#include <time.h>

/**
 * main - Prints single digit numbers of base 10
 * Return: Always (Success)
 *
 */

int main(void)
{
	int x;

	for (x = 0; x < 10; x++)
	printf("%d", x);
	printf("\n");

	return (0);
}
